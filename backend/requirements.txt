# Web框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.20

# 数据库
sqlalchemy>=2.0.25
asyncpg>=0.29.0    # PostgreSQL异步驱动
psycopg2-binary>=2.9.9  # PostgreSQL同步驱动（用于迁移脚本）
alembic>=1.14.0  # 数据库迁移工具
aiosqlite>=0.22.0

# 数据验证
pydantic>=2.10.0
pydantic-settings>=2.8.0

# AI服务
openai>=1.50.0
anthropic>=0.70.0

# 工具库
httpx>=0.28.0
python-dotenv>=1.0.0
psutil>=6.0.0

# MCP官方库（Model Context Protocol Python SDK）
# 本地开发使用 Python 3.8 时需要注释此行
# Docker 环境使用 Python 3.12，可以正常安装
mcp>=1.0.0

# 向量数据库和Embedding (长期记忆系统)
chromadb>=0.5.0

# Transformers（兼容 Python 3.8+）
transformers>=4.40.0,<5.0.0

# Sentence Transformers（兼容 Python 3.8+）
sentence-transformers>=3.0.0

# 可选：加速推理（如果需要 GPU 支持）
# torch>=2.0.0  # 通过 Dockerfile 单独安装以支持多架构
